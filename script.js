const menuBtn=document.querySelector('.menu-btn');const navLinks=document.querySelector('.nav-links');menuBtn.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuBtn.setAttribute('aria-expanded',open)});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>navLinks.classList.remove('open')));const filters=document.querySelectorAll('[data-filter]');filters.forEach(btn=>btn.addEventListener('click',()=>{const cat=btn.dataset.filter;document.querySelectorAll('.filters button').forEach(b=>b.classList.toggle('active',b.dataset.filter===cat));document.querySelectorAll('.article').forEach(card=>card.classList.toggle('hidden',cat!=='all'&&card.dataset.category!==cat))}));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const briefForm=document.querySelector('#briefForm');if(briefForm){briefForm.addEventListener('submit',async e=>{e.preventDefault();const text=`طلب مشروع جديد\nالاسم: ${document.querySelector('#name').value}\nالخدمة: ${document.querySelector('#service').value}\nالتفاصيل: ${document.querySelector('#goal').value}`;const status=document.querySelector('#formStatus');try{await navigator.clipboard.writeText(text);status.textContent='تم نسخ التفاصيل — يمكنك إرسالها عبر وسيلة التواصل المناسبة.'}catch{status.textContent='تعذر النسخ تلقائيًا. حدّد النص وانسخه يدويًا.'}})}
